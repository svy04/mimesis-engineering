#!/usr/bin/env node

import fs from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";

const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");
const outputRoot = path.join(root, ".mimesis", "first-loop-demo");
const mimesisDir = path.join(outputRoot, ".mimesis");

function write(relativePath, content) {
  const fullPath = path.join(outputRoot, relativePath);
  fs.mkdirSync(path.dirname(fullPath), { recursive: true });
  fs.writeFileSync(fullPath, content);
}

const weakArtifact = fs.readFileSync(path.join(root, "examples", "weak-readme.md"), "utf8").trim();

const files = new Map([
  ["README.md", `# First Loop Demo

Status: completed local demo.

This folder is generated by \`npm run first-loop:demo\`.
It demonstrates one local Mimesis loop from a weak README fixture to a stronger proof-bounded README.

Run:

\`\`\`bash
npm run workspace:check -- .mimesis/first-loop-demo
npm run case:check -- .mimesis/first-loop-demo
\`\`\`

Boundary:
This demo proves only that the local first-loop artifact trail can be generated and checked.
It does not prove external adoption, benchmarked productivity, customer outcomes, legal originality, or a real external case.
`],
  ["weak-artifact.md", `# Weak Artifact

Source: \`examples/weak-readme.md\`

\`\`\`markdown
${weakArtifact}
\`\`\`
`],
  [".mimesis/spec_lock.md", `# Spec Lock

Case Status: completed local demo.

## Goal

Transform \`examples/weak-readme.md\` into a clearer first-action README fixture.

## Standard

Give AI standards, not roles.
Bring one weak artifact.

## Boundaries

- Do not copy reference-pack wording.
- Do not claim external adoption.
- Do not claim benchmarked productivity.
- Do not present this fixture as a real customer or submitter artifact.

## Proof Standard

The demo is locally complete only when \`workspace:check\` and \`case:check\` pass on this folder.
`],
  [".mimesis/procedure_tree.md", `# Procedure Tree

Case Status: completed local demo.

1. Import the weak README fixture.
2. Distill the audience, goal, and forbidden claims.
3. Capsule the change into promise, first action, and proof boundary.
4. Shard the artifact trail into the required \`.mimesis\` files.
5. Verify with \`workspace:check\` and \`case:check\`.
6. Remember the result and remaining proof gap.
`],
  [".mimesis/artifact-brief.md", `# Artifact Brief

## Artifact

\`examples/weak-readme.md\`

## Audience

A new builder deciding whether to run a Mimesis loop.

## Goal

Make the first action clear and keep proof claims bounded.

## Current Weakness

The weak fixture says the project helps with AI work but does not say what to do first or what remains unproven.

## Constraint

Preserve the local-demo boundary and avoid adoption, benchmark, customer, legal, or publication claims.

## Success Looks Like

The improved README fixture names the promise, first command, evidence path, and proof boundary.

## Proof Needed

\`workspace:check\`, \`case:check\`, and this generated artifact trail.
`],
  [".mimesis/reference-set.md", `# Reference Set

## Use Case

Improve a weak README fixture with the Mimesis GitHub README reference pack.

## References

| Source Type | Source / Reference | Why It Is Strong | What To Inspect | What Not To Copy | Claim Boundary |
| --- | --- | --- | --- | --- | --- |
| local reference pack | \`reference-packs/github-readme.md\` | Captures README structure standards | promise, quickstart, proof limits, contribution path | exact wording or implied authority | local structure only |
| local framework spec | \`spec/mimesis-v0.1.md\` | Defines the v0.1 loop contract | artifact trail and proof boundary | stronger public claims | local framework evidence |

## Selection Standard

Use source-first artifacts already present in this repository before inventing a new onboarding pattern.

## Missing References

A future external case should add real project-specific reference artifacts.

## Attribution Notes

The demo borrows structure categories, not wording.

## Source-First Check

- Local weak artifact inspected.
- Local reference pack named.
- Framework spec named.
- Secondary commentary not used as proof.
`],
  [".mimesis/structure-map.md", `# Structure Map

## Knowledge Structure

The improved README needs a promise, first action, evidence path, and proof boundary.

## User Flow

Reader sees the weak artifact, sees the improved artifact, then runs local checks.

## Trust Devices

The demo points to generated files and explicitly names what is not proven.

## Claim Boundary

This demo proves local repeatability of the first-loop trail only.

## Failure Handling

If the demo is described as external proof, benchmark proof, or customer proof, the claim is unsafe.
`],
  [".mimesis/transformation-plan.md", `# Transformation Plan

## Transfer

Transfer the reference pack's sequence: promise, quickstart, evidence, boundary.

## Change

Rewrite the weak fixture into a concrete first-action README.

## Reject

Reject copied wording, fake proof, broad productivity claims, and hidden references.

## Proof Needed

The generated case must pass \`workspace:check\` and \`case:check\`.
`],
  [".mimesis/improved-artifact.md", `# Improved Artifact

## Improved Version

# Mimesis First Loop

Bring one weak artifact.
Study one reference pack.
Transform the artifact under a visible proof boundary.

First action:

\`\`\`bash
npm run first-loop:demo
npm run audit:first-loop
\`\`\`

The generated demo shows the artifact brief, reference set, structure map, transformation plan, improved artifact, boundary check, case note, and run ledger.

## Why It Is Stronger

It is stronger because it replaces vague benefit language with a concrete action, named evidence, and explicit limits.

## What Remains Unproven

External adoption, benchmarked productivity, customer outcomes, legal originality, package publication, and real external proof remain unproven.
`],
  [".mimesis/boundary-check.md", `# Boundary Check

## Surface Similarity Risk

Low. The improved fixture uses generic README moves and does not copy protected reference wording.

## Wording Risk

Low. The wording is original to the demo.

## Layout Risk

Low. The headings are conventional and functional.

## Authority Borrowing Risk

Medium. A local reference pack must not be described as external validation.

## Proof Risk

High if the demo is described as real external proof or adoption.

## Attribution Needed?

Name \`reference-packs/github-readme.md\` and \`spec/mimesis-v0.1.md\` as local references.

## What Was Actually Transformed?

A vague README fixture became a first-action README with local evidence and proof limits.

## What Remains Unproven?

External adoption, benchmarked productivity, customer outcomes, legal originality, package publication, and real external proof remain unproven.
`],
  [".mimesis/case-note.md", `# Case Note

## Starting Artifact

\`examples/weak-readme.md\`

## Problem

The starting artifact was vague, overbroad, and did not offer a first action.

## Reference Artifacts

- \`reference-packs/github-readme.md\`
- \`spec/mimesis-v0.1.md\`

## Extracted Structure

Promise, first action, evidence path, and proof boundary.

## Transformation

The weak README fixture was rewritten into a concise first-loop entry point with commands and limits.

## Before / After

Before:
The weak README said the project helps with AI work, told the reader to run it somehow, and implied broad team benefit.

After:
The improved artifact says what to bring, what to study, which commands to run, and which claims remain unproven.

## What Improved

The artifact became clearer, more actionable, and safer to cite because it names the first action and the proof boundary.

## What Remains Unproven

External adoption, benchmarked productivity, customer outcomes, legal originality, package publication, and real external proof remain unproven.

## Next Proof Artifact

Run the same loop on a permissioned external weak artifact.
`],
  [".mimesis/run_ledger.md", `# Run Ledger

## Run

Date: 2026-06-10

Operator: \`npm run first-loop:demo\`

Artifact: \`examples/weak-readme.md\`

## Import

- Imported the weak README fixture.
- Imported \`reference-packs/github-readme.md\`.
- Imported \`spec/mimesis-v0.1.md\`.

## Distill

- Goal: make the first action and proof boundary visible.
- Exclusions: no external adoption, benchmark, customer, legal, package, or publication claim.

## Capsule

- Promise.
- First action.
- Evidence path.
- Proof boundary.

## Shard

- Generated completed \`.mimesis\` protocol files.
- Generated the weak artifact copy.
- Generated a case note.

## Verify

- Intended checks: \`workspace:check .mimesis/first-loop-demo\` and \`case:check .mimesis/first-loop-demo\`.
- The audit writes \`.mimesis/case-proof.md\` inside the demo.

## Remember

- This is local first-loop demo evidence.
- It does not replace a real permissioned external case.
`],
]);

if (fs.existsSync(outputRoot)) {
  fs.rmSync(outputRoot, { recursive: true, force: true });
}

for (const [relativePath, content] of files) {
  write(relativePath, content);
}

fs.mkdirSync(mimesisDir, { recursive: true });

console.log(`[first-loop-demo] ${path.relative(root, outputRoot).replaceAll(path.sep, "/")}`);
